<div class="container">
	<div class="row">
		<div class="col-md-12">
			<%= link_to "< All Pages", pages_path %>
			<h1><%= @page.title %></h1>
			<%= link_to "Edit", edit_page_path(@page) %>
			<p><%= @page.body %></p>
		</div>
	</div>
</div>
			